using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyMovementScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // GameObject londonToBostonPlane = (GameObject)Instantiate(Resources.Load("Enemy Plane"));
        // londonToBostonPlane.transform.position = new Vector3(1000,1000,1000);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
